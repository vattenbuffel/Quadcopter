Maybe implement a safety feature which stops the motor when an angle is out of bound

Send error messages via mqtt to the topic "error"

Try to estimate location in space. Start doing this using only the imu and see how that works